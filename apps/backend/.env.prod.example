# Production environment variables template
# Copy this file to .env.prod and fill in real values before running docker-compose
# NEVER commit .env.prod with real secrets to version control

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=norbertsSpark
POSTGRES_INITDB_ARGS=-E UTF8 --locale=en_US.UTF-8
DATABASE_URL=postgresql://postgres:change_me_in_production@postgres:5432/norbertsSpark
DATABASE_SSL_ENABLED=false
DATABASE_SSL_REJECT_UNAUTHORIZED=true
DATABASE_CONNECTION_TIMEOUT_MS=5000
DATABASE_IDLE_TIMEOUT_MS=30000
DATABASE_POOL_MAX=20
DATABASE_POOL_MIN=5
DATABASE_POOL_MAX_LIFETIME_SECONDS=60

# Redis
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
REDIS_URL=redis://:change_me_in_production@redis:6379
REDIS_PASSWORD=change_me_in_production

# Server
USE_HTTPS=false
NODE_ENV=production
PORT=3001
HOST=0.0.0.0

# Auth
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=change_me_in_production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
OAUTH_SYNC_SECRET=change_me_in_production
JWT_ISSUER=norberts-spark
JWT_EXPIRATION=3600
REFRESH_TOKEN_EXPIRATION=604800
ACCESS_TOKEN_BUFFER=300

# Logging
LOG_LEVEL=info
API_VERSION=v1

# CORS - comma-separated list of allowed origins
ALLOWED_ORIGINS=https://your-production-domain.com

# AI - https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=your_google_generative_ai_api_key_here
GOOGLE_API_KEY=your_google_api_key_here
MODEL_NAME=gemini-2.5-flash

# Email - https://resend.com/
RESEND_API_KEY=your_resend_api_key_here
EMAIL_FROM_ADDRESS=onboarding@resend.dev

# Upstash Redis
UPSTASH_REDIS_REST_URL=https://placeholder.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here

# Sentry - https://sentry.io
SENTRY_DSN=your_sentry_dsn_here
SENTRY_ENABLED=false
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project

# Cloudflare
CLOUDFLARE_ACCESS_ID=your_cloudflare_access_id_here
CLOUDFLARE_ACCESS_SECRET=your_cloudflare_access_secret_here
CLOUDFLARE_API=your_cloudflare_api_token_here
CLOUDFLARE_ENDPOINT=your_cloudflare_endpoint_here
BUCKET=your_cloud_bucket_name_here

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here

# Encryption key for 2FA secrets
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
ENCRYPTION_KEY=change_me_in_production

# OpenTelemetry
OTEL_CAPTURE_QUERY_TEXT=false

# Application
SERVICE_NAME=norberts-spark-backend
APPLICATION_NAME=Norbert's Spark Backend
